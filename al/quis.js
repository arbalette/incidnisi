let out = ''; // Initialize the `out` variable

// Check if `out` exists and has a value
if (out) {
    out += ','; // Append a comma to the existing value of `out`
}

// Example usage
console.log(out); // Outputs ","
